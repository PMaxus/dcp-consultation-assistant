# Screenshots (to be added)

`architecture.svg` is generated and used in the README. The screenshots below are
placeholders — drop the image files here with these names and they will line up with the
README references. **All screenshots must use synthetic / anonymized data and must not
show any client-identifying details** (client name, real prices, contacts).

| File | What it should show |
|---|---|
| `catalog.png` | A fragment of the service catalog / pricing rules (Google Sheets or PostgreSQL) — field structure and a few sample rows. |
| `session-storage.png` | A request stored in PostgreSQL: one session and how the conversation is kept in memory. |
| `dialog.png` | A Telegram dialog of the assistant with a client (consultation + calculation), with anonymized content. |
| `issue-tracker.png` | The PostgreSQL issue tracker (via NocoDB): issue structure, statuses, handling history. |
